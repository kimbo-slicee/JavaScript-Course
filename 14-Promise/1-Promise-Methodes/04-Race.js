/*
* Description
* The Promise.race() method is one of the promise concurrency methods. It's useful when you want the first async task to
* complete, but do not care about its eventual state (i.e. it can either succeed or fail).
* */